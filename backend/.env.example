# Port the backend server listens on
PORT=4000

# The frontend's dev-server URL, used for CORS.
# Vite's default dev port is 5173 - change this if yours differs.
FRONTEND_ORIGIN=http://localhost:5173
